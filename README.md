# Search Crawler
Hello! This project, written in go, is a simple search crawler. Currently, it is being developed to compare a DFS vs BFS crawling algorithm, and in the future I plan to add SQL functionality for better data aggregation.
